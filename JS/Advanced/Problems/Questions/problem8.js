// Problem: Assuming the fibonacci sequence starts with [0, 1, 1], find the nth value of the sequence

function findNthValueInSequence(n) {

}

// Hint: this can be done both recursively or iteratively